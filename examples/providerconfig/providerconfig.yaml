apiVersion: ovh.saagie.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: example-creds-ovh
      namespace: crossplane-system
      key: credentials
